services:
  lakehouse:
    build: .
    volumes:
      - .:/app
      - ./data:/app/data
    env_file: .env
    ports:
      - "3001:3000"
      - "8502:8501"
      - "8001:8000"
    restart: always
    command: bash /app/start.sh
