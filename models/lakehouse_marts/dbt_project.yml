name: 'lakehouse_marts'
version: '1.0.0'
profile: 'lakehouse_marts'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  lakehouse_marts:
    silver:
      +materialized: table
      +schema: silver
    gold:
      +materialized: table
      +schema: gold
